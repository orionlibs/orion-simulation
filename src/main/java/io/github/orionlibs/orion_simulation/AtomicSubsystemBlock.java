package io.github.orionlibs.orion_simulation;

/**
 * Blocks within an atomic subsystem block that references a model
 * execute as a single unit (atomic execution) at each time step
 */
public class AtomicSubsystemBlock extends SubsystemBlock
{
}
